import template from "./Admin.html";

import { fromRecord, pb, toRecord } from "../util.js";

export default {
  template: template,
  data() {
    return {
      loggedIn: false,
    };
  },

  mounted() {
  },

  methods: {
  },
};
